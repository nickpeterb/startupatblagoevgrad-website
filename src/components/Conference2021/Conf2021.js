import React, {useEffect} from 'react';

export default function Conf2021() {
    useEffect(() => {
        window.scrollTo(0, 0);
    }, []);

    return(
        <div>
            
        </div>
    );
}